{% extends "layouts/base.njk" %}

{% block page_title %}Web Components Club article{% endblock %}

{% block content %}
    <main id="main-content">
        <article>
            <h2>{{ title }}</h2>
            <p class="date"><time datetime="{{ date }}">{{ date | date }}</time></p>
            {{ content | safe }}
        </article>
    </main>
{% endblock %}
